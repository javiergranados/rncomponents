import { StyleSheet } from 'react-native';

const appStyles = StyleSheet.create({
  gobalMargin: {
    marginHorizontal: 20,
  },
  title: {
    fontSize: 35,
    fontWeight: 'bold',
  },
});

export { appStyles };
